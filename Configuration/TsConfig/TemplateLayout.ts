
tx_addressmgmt.templateLayouts {
  short = Referenzen Kurz-Liste (short)
  # never rename the grid... - 1st 4 letters are used for layout-switch in General.html
  gridRef = Referenzen Kacheln (gridRef)
  gridNews = News Kacheln (gridNews)
}